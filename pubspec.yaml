name: zenith_architecture
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  equatable:
  dio: ^5.9.2
  get_it: ^7.6.0
  internet_connection_checker_plus: ^2.5.1
  dartz: ^0.10.1
  flutter_bloc:
  hive_flutter:
  skeletonizer: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner:
  hive_generator: 
  mocktail: ^1.0.4


flutter:
  uses-material-design: true
